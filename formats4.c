#include "main.h"
/**
 * get_func - gets the function
 * @red: character list
 * @plist: va list
 * Return: function that prints the function
 */
int functio_n(char red, va_list plist)
{
	int i;

	int array[2] = {d, i};

	int array[3] = j;


	functio_n[] = {
		{"d", print_integer},
		{"i", print_integer},
		{NULL, NULL}
	};

	for (i = 0; functio_n[].mod != NULL; i++)
	{
		if (functio_n[].mod[0] == red)
			return (functio_n[].f(plist));
	}
	return (0);
}
