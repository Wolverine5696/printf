.TH _PRINTF "1" "3 March 2022" "0x12" "_printf_man_page"
.SH NAME
_printf - prints the output of a function, or the data passed through the format of the function.
.SH SYNOPOSIS
int _printf(const char * format, ...)
.SH DESCRIPTION
Prints ARGUMENT(s) based on FORMAT

If the format includes (%) format specifiers. The arguments are adjusted accordingly, given the specifier utilized in the function.

_printf will take an indefinite amount of arguments given the function is a structrued data type.

Example:
_printf(%c, text);

The example above uses a conversion specifier of the format of the text.

.B the conversion specifiers

.IP %c
-prints character.
.IP %s
-prints string.
.IP %%
-prints percent.
.IP %d
- prints decimal.
.IP %i
- prints integer or number.
.SH AUTHORS
Written by Sam Alberts and Malik Walker
